# Programming 수업 소스 코드
## 02_counter
vanila HTML, CSS, JavaScript로 만든 Counter
---
## 2026.03.24 React 시작
```shell
npm create vite@latest .
```

### CounterApp
- `useState()`
- `onClick={()=> set함수()}`
- `onClick={()=> set함수((이전state) => 이전state + 1)}`

### TodoListApp
- React Component 분리
- for -> htmlFor, class -> className
- props
- `<input id={id} value={} />`, `<label htmlFor={id} />`
- onChange=
- 구조 분해 할당
- `...연산자`
- `<form onSubmit={} />`
- `덩어리.map()`
- `<TodoItem key={} />`
- `const handleEvent = (event) => {}`
- `{조건식 ? 참 : 거짓}`
- `{조건식 && 참}`
- `{!조건식 && 거짓}`
- onkeyDown
- LocalStorage, `useEffect()`